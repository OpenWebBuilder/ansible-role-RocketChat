---
deploy_method: "Docker"

release: "{{ latest_release }}"

root_url: "{{ test_domain }}"

ip: "{{ ansible_default_ipv4.address }}"

host_port: "{{ default_port }}"


# Optional: bash_history
devel: false